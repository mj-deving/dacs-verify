{
  "name": "dacs-verify",
  "version": "0.0.1",
  "description": "Independent third-party reference verifier for the DACS (Demos Agent Commerce Standards) v0.1 — JCS canonicalization, content hashing, §7.7 domain-separated signatures, DACS-1 listing validation, DACS-5 bundle verification.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "bin": {
    "dacs-verify": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "conformance": "bun conformance/run.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
